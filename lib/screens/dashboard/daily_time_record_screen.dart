import 'package:flutter/material.dart';

class DailyTimeRecordScreen extends StatelessWidget {
  const DailyTimeRecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Daily Time Record")),
      body: Center(child: Text("Daily Time Record Page")),
    );
  }
}