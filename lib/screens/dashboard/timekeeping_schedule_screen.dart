import 'package:flutter/material.dart';

class TimekeepingScheduleScreen extends StatelessWidget {
  const TimekeepingScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Timekeeping Schedule")),
      body: Center(child: Text("Timekeeping Schedule Page")),
    );
  }
}